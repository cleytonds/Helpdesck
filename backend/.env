# ==========================================
# Configuracao do Backend HelpDesk
# ==========================================

# MySQL (XAMPP)
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=
DB_NAME=helpdesk

# Servidor HTTP
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# JWT
JWT_SECRET=helpdesk_secret_key_2024
JWT_EXPIRES_HOURS=24

# CORS
CORS_ORIGIN=*
CORS_METHODS=GET,POST,OPTIONS
CORS_HEADERS=Content-Type,Authorization

# Modo (debug | release)
MODE=debug

