// ======================================================
// router.cpp
// Aqui serão registradas todas as rotas da API
// (auth, tickets, admin)
// ======================================================

#include <iostream>
#include "router.h"

// ======================================================
// Inicializa rotas da aplicação
// ======================================================
void Router::initRoutes() {

    std::cout << "Registrando rotas..." << std::endl;

    // Aqui depois vamos adicionar:
    // POST /register
    // POST /login
    // GET  /tickets
    // POST /tickets
    // GET  /admin/tickets

    std::cout << "Rotas carregadas com sucesso!" << std::endl;
}